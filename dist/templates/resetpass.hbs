<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .container {
      border: 1px solid #ddd;
      border-radius: 5px;
      padding: 20px;
      background-color: #f9f9f9;
    }
    .header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 1px solid #eee;
      margin-bottom: 20px;
    }
    .logo {
      max-width: 150px;
      height: auto;
      margin-bottom: 15px;
    }
    .button {
      display: inline-block;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 4px;
      margin-top: 20px;
      margin-bottom: 20px;
      font-weight: bold;
    }
    .button:hover {
      background-color: #45a049;
    }
    .alternative-link {
      word-break: break-all;
      color: #666;
      margin-top: 15px;
      font-size: 14px;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 12px;
      color: #777;
      text-align: center;
    }
    .security-note {
      font-size: 13px;
      color: #666;
      margin-top: 25px;
      padding: 15px;
      background-color: #f5f5f5;
      border-left: 3px solid #999;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      {{#if companyLogo}}
        <img src="{{companyLogo}}" alt="Company Logo" class="logo">
      {{/if}}
      <h1>Reset Your Password</h1>
    </div>
    
    <p>Hello,</p>
    
    <p>We received a request to reset your password for your invoice management account. Please click the button below to create a new password:</p>
    
    <div style="text-align: center;">
      <a href="{{link}}" class="button">Reset Password</a>
    </div>
    
    <p class="alternative-link">If the button doesn't work, you can copy and paste this URL into your browser: <br>{{link}}</p>
    
    <div class="security-note">
      <p><strong>Security Note:</strong> If you didn't request this password reset, please ignore this email or contact support immediately if you believe your account may be compromised.</p>
    </div>
    
    <div class="footer">
      <p>&copy; {{currentYear}} Invoice Management System. All rights reserved.</p>
      {{#if socialLinks}}
        <p>
          {{#each socialLinks}}
            <a href="{{this.url}}" style="margin: 0 10px;">{{this.name}}</a>
          {{/each}}
        </p>
      {{/if}}
    </div>
  </div>
</body>
</html>
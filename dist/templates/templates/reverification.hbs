<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify Your Email Change</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .container {
      border: 1px solid #ddd;
      border-radius: 5px;
      padding: 20px;
      background-color: #f9f9f9;
    }
    .header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 1px solid #eee;
      margin-bottom: 20px;
    }
    .logo {
      max-width: 150px;
      height: auto;
      margin-bottom: 15px;
    }
    .button {
      display: inline-block;
      background-color: #2196F3;
      color: white;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 4px;
      margin-top: 20px;
      margin-bottom: 20px;
      font-weight: bold;
    }
    .button:hover {
      background-color: #0b7dda;
    }
    .alternative-link {
      word-break: break-all;
      color: #666;
      margin-top: 15px;
      font-size: 14px;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 12px;
      color: #777;
      text-align: center;
    }
    .security-note {
      font-size: 13px;
      color: #666;
      margin-top: 25px;
      padding: 15px;
      background-color: #f5f5f5;
      border-left: 3px solid #999;
    }
    .info-box {
      background-color: #e8f4fd;
      border-left: 3px solid #2196F3;
      padding: 15px;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      {{#if companyLogo}}
        <img src="{{companyLogo}}" alt="Company Logo" class="logo">
      {{/if}}
      <h1>Verify Your Email Change</h1>
    </div>
    
    <p>Hello,</p>
    
    <p>You have requested to change your email address for your invoice management account. To complete this process and ensure the security of your account, please verify this new email address by clicking the button below:</p>
    
    <div style="text-align: center;">
      <a href="{{link}}" class="button">Verify Email Change</a>
    </div>
    
    <p class="alternative-link">If the button doesn't work, you can copy and paste this URL into your browser: <br>{{link}}</p>
    
    <div class="info-box">
      <p><strong>Note:</strong> After verification, this email will become your primary contact email for all account notifications and communications.</p>
    </div>
    
    <div class="security-note">
      <p><strong>Security Note:</strong> If you did not request this email change, please contact support immediately as someone may be attempting to access your account.</p>
    </div>
    
    <div class="footer">
      <p>&copy; {{currentYear}} Invoice Management System. All rights reserved.</p>
      {{#if socialLinks}}
        <p>
          {{#each socialLinks}}
            <a href="{{this.url}}" style="margin: 0 10px;">{{this.name}}</a>
          {{/each}}
        </p>
      {{/if}}
    </div>
  </div>
</body>
</html>